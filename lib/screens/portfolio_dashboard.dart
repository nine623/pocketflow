import 'package:flutter/material.dart';

class PortfolioDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Portfolio Dashboard"),
      ),
      body: const Center(
        child: Text("Dashboard ทำงานแล้ว"),
      ),
    );
  }
}
